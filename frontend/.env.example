# ============================================================
# Contoh environment variables untuk FRONTEND (Next.js).
# Salin ke ".env.local" (lokal) atau set di Coolify.
# ============================================================

# Alamat backend FastAPI yang diakses browser.
# - Lokal (dev)         : http://127.0.0.1:8000
# - Deploy via IP+port  : http://160.187.144.188:8000
# - Deploy via domain   : https://roadtierbers.pocari.id/api  (bila pakai path routing)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
